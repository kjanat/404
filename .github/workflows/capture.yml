name: Capture Animation
on:
  workflow_dispatch:
    inputs:
      width:
        description: Capture width in pixels
        required: false
        default: "1152"
      height:
        description: Capture height in pixels
        required: false
        default: "648"
      url:
        description: URL to capture
        required: false
        default: "https://unavailable.kajkowalski.nl"
      duration:
        description: Capture duration in seconds
        required: false
        default: "6"
      fps:
        description: Frames per second
        required: false
        default: "12"
      out:
        description: Output filename (.webp or .gif)
        required: false
        default: "preview.webp"
permissions: { contents: read }
jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
      - run: bun i
      - name: Install Playwright browser
        run: bunx playwright install chromium
      - name: Generate Animation
        run: bun capture -w "${{ inputs.width }}" -h "${{ inputs.height }}" --url "${{ inputs.url }}" -d "${{ inputs.duration }}" --fps "${{ inputs.fps }}" -o "${{ inputs.out }}"
      - uses: actions/upload-artifact@v6
        with: { name: "${{ inputs.out }}", path: "${{ inputs.out }}" }
