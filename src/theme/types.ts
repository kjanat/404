/** User-facing theme preference. `system` follows OS scheme. */
export type ThemePreference = 'system' | 'light' | 'dark';

/** Concrete theme value applied to DOM attributes. */
export type ThemeName = 'light' | 'dark';
