/** Legacy re-export for cloud background generator. */
export { generateCloudBackground } from './storm/clouds.ts';

/** Legacy re-export for procedural storm runtime. */
export { StormEngine } from './storm/engine.ts';
